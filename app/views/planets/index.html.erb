

<%= simple_form_for :search, url: root_path, id: 'search-bar', method: :get do |f| %>
  <div class="search-form-control form-group m-5">
    <input class="form-control string required" type="text" name="search[query]" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> 🥸 Look for planets 🥸
    </button>
  </div>
<% end %>


<div class="planet-cards m-5">
  <% @planets.each do |p| %>
  <div class='planet-card'>
    <% if p.photo.attached? %>
        <%= cl_image_tag(p.photo.key)%>
      <% end %>

    <div class="planet-card-infos">
      <div>
        <h2> <%= link_to p.name, planet_path(p.id) %> </h2>
        <p> <%= p.description %> </p>
      </div>
      <h2 class="planet-card-pricing">£ <%= p.price %> </h2>
      <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
    </div>
  </div>
  <% end %>
</div>
